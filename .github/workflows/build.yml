name: Build Modules
on:
  workflow_call:
    inputs:
      from_ci:
        type: boolean
        required: false
        default: true

  workflow_dispatch:

jobs:
  run:
    permissions: write-all
    runs-on: ubuntu-latest
    steps:
      - uses: actions/setup-java@v4
        with:
          distribution: "zulu"
          java-version: "17"

      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          submodules: true

      - name: Update config
        if: ${{ inputs.from_ci }}
        run: |
<<<<<<< HEAD
          # Setup environment
          source utils.sh
          set_prebuilts

          # Auto-update app versions from constants.kt (always, regardless of build.md)
          toml_prep config.toml
          if auto_update_app_versions "config.toml" "$(toml_get "$(toml_get_table_main)" patches-source)"; then
            echo "App versions updated in config.toml"
          fi

          # Reload config after potential app version updates
          toml_prep config.toml

          # Load build.md from update branch if it exists (for patches updates check)
          if git show origin/update:build.md > build.md 2>/dev/null; then
            # Generates config.json if there are patches updates
=======
          if git checkout origin/update build.md; then
>>>>>>> e42b4c7764bdb69ea640fd0da93401398028cea8
            UPDATE_CFG=$(./build.sh config.toml --config-update)
            if [ "$UPDATE_CFG" ]; then
              echo "$UPDATE_CFG" > config.json
            fi
          fi

<<<<<<< HEAD
      - name: Make release name and get version
        id: release_info
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          TODAY=$(date +'%Y-%m-%d')
          BRANCH=${{ github.ref_name }}

          # Get all numeric tags, find highest, add 1
          MAX_TAG=$(gh release list --limit 100 | awk -F '\t' '{print $3}' | grep -E '^[0-9]+$' | sort -rn | head -1)
          if [ -z "$MAX_TAG" ]; then MAX_TAG=0; fi
          NEXT_VER_CODE=$((MAX_TAG + 1))

          # Count releases for this date to get index
          COUNT=$(gh release list --limit 100 | grep "$TODAY" | wc -l)
          INDEX=$((COUNT + 1))

          # Create release name based on branch
          if [ "$BRANCH" = "dev" ]; then
            NAME="ReVanced Extended - $TODAY [dev] (v$INDEX)"
          else
            NAME="ReVanced Extended - $TODAY (v$INDEX)"
          fi

          echo "NAME=$NAME" >> $GITHUB_OUTPUT
          echo "TAG=$NEXT_VER_CODE" >> $GITHUB_OUTPUT
          echo "NEXT_VER_CODE=$NEXT_VER_CODE" >> $GITHUB_OUTPUT
=======
      - name: Get next version code
        id: next_ver_code
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          TAG=$(gh release list -L 1 | awk -F '\t' '{print $3}')
          if [ -z "$TAG" ]; then TAG=0; fi
          echo "NEXT_VER_CODE=$((TAG + 1))" >> $GITHUB_OUTPUT
>>>>>>> e42b4c7764bdb69ea640fd0da93401398028cea8

      - name: Build modules/APKs
        run: if [ -f "config.json" ]; then ./build.sh config.json; else ./build.sh config.toml; fi
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GITHUB_REPOSITORY: $GITHUB_REPOSITORY
<<<<<<< HEAD
          NEXT_VER_CODE: ${{ steps.release_info.outputs.NEXT_VER_CODE }}

      - name: Commit config after successful build
        if: ${{ inputs.from_ci }}
        run: |
          git add config.toml
          if git diff --cached --quiet; then
            echo "No config changes to commit."
          else
            git config user.name "github-actions[bot]"
            git config user.email "github-actions[bot]@users.noreply.github.com"
            git commit -m "Auto-update config.toml [CI]"
            git push origin HEAD:$GITHUB_REF_NAME
          fi
=======
          NEXT_VER_CODE: ${{ steps.next_ver_code.outputs.NEXT_VER_CODE }}
>>>>>>> e42b4c7764bdb69ea640fd0da93401398028cea8

      - name: Get output
        id: get_output
        run: |
          DELIM="$(openssl rand -hex 8)"
          echo "BUILD_LOG<<${DELIM}" >> "$GITHUB_OUTPUT"
          cat build.md >> "$GITHUB_OUTPUT"
          echo "${DELIM}" >> "$GITHUB_OUTPUT"
          cp -f build.md build.tmp

      - name: Upload modules to release
        uses: svenstaro/upload-release-action@v2
        with:
          body: ${{ steps.get_output.outputs.BUILD_LOG }}
          repo_token: ${{ secrets.GITHUB_TOKEN }}
          file: ./build/*
<<<<<<< HEAD
          release_name: ${{ steps.release_info.outputs.NAME }}
          tag: ${{ steps.release_info.outputs.TAG }}
          file_glob: true
          overwrite: false
          prerelease: ${{ github.ref_name != 'main' }}

      - name: Update changelog and Magisk update json
        id: update_config
        if: github.ref_name == 'main'
=======
          release_name: ReVanced
          tag: ${{ steps.next_ver_code.outputs.NEXT_VER_CODE }}
          file_glob: true
          overwrite: true

      - name: Update changelog and Magisk update json
        id: update_config
>>>>>>> e42b4c7764bdb69ea640fd0da93401398028cea8
        run: |
          git checkout -f update || git switch --discard-changes --orphan update
          cp -f build.tmp build.md
          get_update_json() {
            echo "{
            \"version\": \"$1\",
<<<<<<< HEAD
            \"versionCode\": ${{ steps.release_info.outputs.NEXT_VER_CODE }},
=======
            \"versionCode\": ${{ steps.next_ver_code.outputs.NEXT_VER_CODE }},
>>>>>>> e42b4c7764bdb69ea640fd0da93401398028cea8
            \"zipUrl\": \"$2\",
            \"changelog\": \"https://raw.githubusercontent.com/$GITHUB_REPOSITORY/update/build.md\"
          }"
          }

          cd build || { echo "build folder not found"; exit 1; }
          for OUTPUT in *magisk*.zip; do
            [ "$OUTPUT" = "*magisk*.zip" ] && continue
            ZIP_S=$(unzip -p "$OUTPUT" module.prop)
            if ! UPDATE_JSON=$(echo "$ZIP_S" | grep updateJson); then continue; fi
            UPDATE_JSON="${UPDATE_JSON##*/}"
            VER=$(echo "$ZIP_S" | grep version=)
            VER="${VER##*=}"
<<<<<<< HEAD
            DLURL="$GITHUB_SERVER_URL/$GITHUB_REPOSITORY/releases/download/${{ steps.release_info.outputs.NEXT_VER_CODE }}/${OUTPUT}"
=======
            DLURL="$GITHUB_SERVER_URL/$GITHUB_REPOSITORY/releases/download/${{ steps.next_ver_code.outputs.NEXT_VER_CODE }}/${OUTPUT}"
>>>>>>> e42b4c7764bdb69ea640fd0da93401398028cea8
            get_update_json "$VER" "$DLURL" >"../$UPDATE_JSON"
          done
          cd ..

          find . -name "*-update.json" | grep . || : >dummy-update.json

      - uses: stefanzweifel/git-auto-commit-action@v5
<<<<<<< HEAD
        if: github.ref_name == 'main'
=======
>>>>>>> e42b4c7764bdb69ea640fd0da93401398028cea8
        with:
          branch: update
          skip_checkout: true
          file_pattern: build.md *-update.json
<<<<<<< HEAD
          commit_message: Bump version ${{ steps.release_info.outputs.NEXT_VER_CODE }}
=======
          commit_message: Bump version ${{ steps.next_ver_code.outputs.NEXT_VER_CODE }}
>>>>>>> e42b4c7764bdb69ea640fd0da93401398028cea8

      - name: Report to Telegram
        env:
          TG_TOKEN: ${{ secrets.TG_TOKEN }}
<<<<<<< HEAD
        if: env.TG_TOKEN != null && github.ref_name == 'main'
        run: |
          cd build || { echo "build folder not found"; exit 1; }

          TG_CHAT="@ev3rlin_rvx"

          NL=$'\n'
          APKS=""
          MODULES=""
          NOTES=""
          MICROG_URL="https://github.com/ReVanced/GmsCore/releases"
          ZYGISK_DETACH_URL="https://github.com/j-hc/zygisk-detach"

          for OUTPUT in *; do
            DL_URL="$GITHUB_SERVER_URL/$GITHUB_REPOSITORY/releases/download/${{ steps.release_info.outputs.NEXT_VER_CODE }}/${OUTPUT}"
=======
        if: env.TG_TOKEN != null
        run: |
          cd build || { echo "build folder not found"; exit 1; }

          TG_CHAT="@rvc_magisk"
          NL=$'\n'
          APKS=""
          MODULES=""
          for OUTPUT in *; do
            DL_URL="$GITHUB_SERVER_URL/$GITHUB_REPOSITORY/releases/download/${{ steps.next_ver_code.outputs.NEXT_VER_CODE }}/${OUTPUT}"
>>>>>>> e42b4c7764bdb69ea640fd0da93401398028cea8
            if [[ $OUTPUT = *.apk ]]; then
              APKS+="${NL}📦[${OUTPUT}](${DL_URL})"
            elif [[ $OUTPUT = *.zip ]]; then
              MODULES+="${NL}📦[${OUTPUT}](${DL_URL})"
            fi
          done
<<<<<<< HEAD

          MODULES=${MODULES#"$NL"}
          APKS=${APKS#"$NL"}

          NOTES+="${NL}ℹ️ Install [Microg](${MICROG_URL}) for non-root YouTube and YouTube Music APKs"
          NOTES+="${NL}ℹ️ Use [zygisk-detach](${ZYGISK_DETACH_URL}) to detach root RVX YouTube and YouTube Music from Play Store"
          NOTES=${NOTES#"$NL"}

=======
          MODULES=${MODULES#"$NL"}
          APKS=${APKS#"$NL"}

>>>>>>> e42b4c7764bdb69ea640fd0da93401398028cea8
          BODY="$(sed 's/^\* \*\*/↪ \*\*/g; s/^\* `/↪ \*\*/g; s/`/\*/g; s/^\* /\↪/g; s/\*\*/\*/g; s/###//g; s/^- /↪ /g; /^==/d;' ../build.md)"
          MSG="*New build!*

          ${BODY}

<<<<<<< HEAD
          *▼ Installation Notes:*
          ${NOTES}

=======
>>>>>>> e42b4c7764bdb69ea640fd0da93401398028cea8
          *▼ Download Links:*
          Modules:
          ${MODULES}

          APKs:
          ${APKS}
          "
          echo "'$MSG'"
          MSG=${MSG:0:9450}
          POST="https://api.telegram.org/bot${TG_TOKEN}/sendMessage"
          curl -X POST --data-urlencode "parse_mode=Markdown" --data-urlencode "disable_web_page_preview=true" --data-urlencode "text=${MSG}" --data-urlencode "chat_id=${TG_CHAT}" "$POST"
